export type Temperature = {
  temp: number;
};

export type LinkList = {
  work: [Object];
  bonfire: [Object];
};

export type LinkObject = {
  name: string;
  group: string;
  mode: string;
  url: string;
};
